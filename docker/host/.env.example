# margo host — runtime config. No required vars: the host stands up
# clean and the first signup at /signup becomes the superuser.
#
# Copy to `.env` only if you want to override defaults.

# Optional: external port mapping. Internal container port is always
# 7331; this only changes what host-side port forwards in to it.
MARGO_HOST_PORT=7331
